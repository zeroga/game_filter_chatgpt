# Deprecated Path

`memory/scenarios/` 已废弃。

不要读取本目录作为场景入口。

新的场景类型模板目录：

```text
memory/scenario_types/
```

实际场景必须在 profile 下：

```text
memory/profiles/<user_key>/scenarios/<scenario_code>.md
```

游戏推荐前必须先确认：

```text
profile code
scenario code
```
