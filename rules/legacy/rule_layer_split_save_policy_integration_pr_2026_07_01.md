# Legacy: Rule Layer Split Save Policy Integration PR

本文件只作为历史 PR #2 补丁归档，不是活跃规则真源。

PR #2 原目标是把 `rule_layer_split_save_policy_2026_07_01.md` 并入旧的 `memory/current_rules.md` 与 `memory/save_flow.md`。Issue #3 后续要求更完整的三层结构重构，因此旧 PR 方案已被覆盖。

当前有效规则以以下文件为准：

```text
rules/current_rules.md
rules/save_flow.md
rules/governance/rule_change_issue_only.md
rules/governance/rule_memory_layer_separation.md
```

Issue #3 的完整审计记录见：

```text
memory/snapshots/issue_3_rule_conflict_audit.md
```
